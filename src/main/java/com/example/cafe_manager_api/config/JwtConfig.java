package com.example.cafe_manager_api.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class JwtConfig {
    // TODO: bind properties from app.jwt (secret, expiration)
}
